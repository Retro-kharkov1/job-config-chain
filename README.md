# config-template-sync
Jenkins plugin: JSON-shaped config templates (common+env merge patch), template generation for target config files, structural drift validation, and Rojet-style version history/rollback with build-version pinning — all persisted natively in Jenkins storage.
