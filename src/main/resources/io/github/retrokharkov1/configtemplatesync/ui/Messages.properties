# Server-side, admin-facing strings surfaced via hudson.model.Failure (rendered as a plain error
# page by Stapler). Only the highest-value, browser-visible validation messages are externalized
# in this pass — see ConfigSetPage for exact call sites. Generated accessor class: Messages
# (org.jvnet.localizer / maven-localizer-plugin, wired by the org.jenkins-ci.plugins:plugin parent
# POM's existing localizer-maven-plugin execution — no pom.xml change needed).

ConfigSetPage.SaveBlocked=Save blocked: {0}
ConfigSetPage.SaveBlockedMismatchedTypes=Save blocked: mismatched content types in base chain — {0} must all share one content type.
ConfigSetPage.SaveBlockedMalformedBaseChain=Save blocked: malformed base chain — {0}
ConfigSetPage.SaveBlockedInvalidRoot=Save blocked: invalid {0} — content must have a top-level object/root element
ConfigSetPage.SaveBlockedInvalidSyntax=Save blocked: invalid {0} — {1}
