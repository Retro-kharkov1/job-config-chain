# Strings for the per-column table filters (tableToolsBlock.jelly). Carried into the script via
# data-* attributes on the ctsyncTableToolsStrings template, the same pattern the secret/history
# row templates use.
filter.placeholder=Filter
filter.count={0} of {1}
filter.noMatches=No rows match the filter
