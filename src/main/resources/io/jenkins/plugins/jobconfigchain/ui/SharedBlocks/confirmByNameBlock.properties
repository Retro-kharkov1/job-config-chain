# Strings for the shared confirm-by-name dialog (confirmByNameBlock.jelly). The OK label is passed
# per operation by the caller, so a destructive confirmation never reads just "OK"; only the shared
# pieces live here.
confirm.cancelLabel=Cancel
confirm.nameMismatch=The typed name does not match. Nothing was changed.
