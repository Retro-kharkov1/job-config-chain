# Server-side, admin-facing strings surfaced via hudson.model.Failure (rendered as a plain error
# page by Stapler). Only the highest-value, browser-visible validation messages are externalized
# in this pass — see ConfigSetPage for exact call sites. Generated accessor class: Messages
# (org.jvnet.localizer / maven-localizer-plugin, wired by the org.jenkins-ci.plugins:plugin parent
# POM's existing localizer-maven-plugin execution — no pom.xml change needed).

ConfigSetPage.SaveBlocked=Save blocked: {0}
ConfigSetPage.SaveBlockedMismatchedTypes=Save blocked: mismatched content types in base chain — {0} must all share one content type.
ConfigSetPage.SaveBlockedMalformedBaseChain=Save blocked: malformed base chain — {0}
ConfigSetPage.SaveBlockedInvalidRoot=Save blocked: invalid {0} — content must have a top-level object/root element
ConfigSetPage.SaveBlockedInvalidSyntax=Save blocked: invalid {0} — {1}

# Menu/action labels and Pipeline step descriptions. Externalized 2026-09-21: these were hardcoded
# English literals in Java, so no locale could translate them however complete its bundle was.
ConfigTemplates.DisplayName=Config Templates
ConfigTemplatesRootAction.Description=Manage per-project Config Sets and their environment overlays.
Step.Substitute.DisplayName=Substitute real values into a config template for the calling Job's own Config Templates
Step.Validate.DisplayName=Validate config template drift for the calling Job's own Config Templates
Step.Setup.DisplayName=Store config-template parameters for the current build
SecretPath.RejectedActiveValueHoldsRealValue=Cannot mark ''{0}'' as a secret path: the currently-active version (v{1}) already stores a real value there. Replace that value with the ''{2}'' placeholder and save a new version first, then bind the credential.
