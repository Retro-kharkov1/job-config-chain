baseChain.heading=Base chain
column.configKey=Config Key
addBase.title=Add another base Config Set to the chain
addBase.label=Add base config
explicitlyStandalone.label=This env Config Set is explicitly standalone — zero base configs, its own content is the entire effective configuration.
emptyState.noBaseChain=No base Config Sets in this chain yet

# Row controls. Externalized 2026-09-21: these were hardcoded English literals inside the page's
# inline JS, so no locale could translate them however complete its bundle was. They reach the
# script through data-* attributes on the rowStrings template element in baseChainBlock.jelly,
# which is the pattern this plugin already uses for secretRowTemplate/versionRowTemplate.
row.dragHandle.title=Drag to reorder
row.moveUp.title=Move up (higher precedence rows come later)
row.moveDown.title=Move down (later rows win on overlapping keys)
row.remove.title=Remove this base from the chain
row.openInNewTab.title=Open {0} in a new tab
row.resolvedCaption=Resolved content (read-only) — {0}, {1} (v{2})
row.notResolvable=Not yet resolvable — check the project/version picker above.
column.mode=Mode
column.pinnedVersion=Pinned version
