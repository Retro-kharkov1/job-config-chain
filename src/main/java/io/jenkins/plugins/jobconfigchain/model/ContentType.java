package io.jenkins.plugins.jobconfigchain.model;

/** A Config Set's structured content format (see multi-format-content.md's "Content type model" section). */
public enum ContentType {
    JSON,
    XML,
    YAML
}
